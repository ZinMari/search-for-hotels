include ../chevron/chevron

mixin menu-link({text= "Введите текст", hasDropdown= "", href=""})
  - const hrefValue = href ? href : "/mock-address/change-me"
  a(class= "menu-link", href= hrefValue)
    | #{text}
    if hasDropdown
      +chevron({className: "menu-link__arrow"})

