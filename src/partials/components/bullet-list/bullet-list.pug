mixin bullet-list({items= [], title="Title"})
  .bullet-list
    if title
      h3.bullet-list__title= title
    ul.bullet-list__list
      if items.length
        each val in items
          li.bullet-list__item= val
      else
        li.bullet-list__item= "Пустой список"
