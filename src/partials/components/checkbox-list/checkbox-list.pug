include ../checkbox/checkbox

mixin checkbox-list(title= 'Title', btns= [])
  .checkbox-list
    h3.checkbox-list__title= title
    ul.checkbox-list__wrap
      if btns.length
        each btn in btns
          li.checkbox-list__item
            +checkbox(btn.text, btn.isChecked)
      else
        +checkbox("Список пуст", true)