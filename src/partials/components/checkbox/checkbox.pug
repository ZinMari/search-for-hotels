mixin checkbox({text= 'Text', isChecked= true, className=""})
  label(class= `checkbox ${className}`)
    input.checkbox__input(type="checkbox", checked= isChecked, value= text)
    span.checkbox__box
    |#{text}
